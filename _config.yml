theme : jekyll-theme-fire
import { useTheme } from "../core/theme.js";

function ExampleA(): JSX.Element {
  // Access the auto-detected or the user-selected theme
  const theme = useTheme("midnight");
}

function ExampleB(): JSX.Element {
  // Explicitely access the "dark" theme
  const theme = useTheme("dark");
}
